---
import { getMarketplacePages } from "@modules/games/utils/getMarketplacePages";

const pages = getMarketplacePages();
---

{
  pages.map((page) => {
    return (
      <li class="text-xl">
        <a href={page.url}>{page.name}</a>
      </li>
    );
  })
}
